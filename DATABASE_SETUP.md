# การตั้งค่าฐานข้อมูล (Database Setup)

ระบบนี้ใช้ SQLite และมีการตั้งค่าไม่ให้ Git ติดตามไฟล์ `instance/database.db` เพื่อป้องกันการเกิด Conflict เมื่อทำงานร่วมกัน เพื่อนร่วมทีมสามารถสร้างฐานข้อมูลเริ่มต้นในเครื่องตัวเองได้ง่ายๆ โดยใช้ระบบ Migration ดังนี้:

## ขั้นตอนการสร้างฐานข้อมูลเริ่มต้น (Seed Data)

1.  **ตรวจสอบไฟล์ JSON**: ในโปรเจกต์จะมีโฟลเดอร์ `backup/` ที่เก็บข้อมูลเริ่มต้นสำหรับการทดสอบ ได้แก่:
    - `backup/users.json` (ข้อมูลบัญชีผู้ใช้)
    - `backup/criteria.json` (เกณฑ์การให้คะแนนปี 2569)
    - `backup/work_types.json` (ประเภทผลงาน)
    - `backup/timeline.json` (กำหนดการยื่นคำขอ)

2.  **รันสคริปต์ Migration**: เปิด Terminal ในโฟลเดอร์โปรเจกต์แล้วรันคำสั่ง:
    ```bash
    python migrate_data.py
    ```
    _สคริปต์นี้จะอ่านไฟล์จากโฟลเดอร์ `backup/` เพื่อสร้างไฟล์ฐานข้อมูล `instance/database.db` ให้โดยอัตโนมัติ_

## ข้อมูลบัญชีผู้ใช้สำหรับการทดสอบ

เมื่อรันสคริปต์แล้ว คุณสามารถใช้บัญชีต่อไปนี้ในการทดสอบระบบ (รหัสผ่านคือ `123` สำหรับทุกบัญชี):

| Username   | Role           | Name                |
| ---------- | -------------- | ------------------- |
| root       | admin          | ผู้ดูแลระบบ         |
| admin_work | administration | เจ้าหน้าที่งานบุคคล |
| research01 | research       | เจ้าหน้าที่งานวิจัย |
| board01    | committee      | คณะกรรมการประจำคณะ  |
| user01     | applicant      | ผศ. สมชาย           |
| user02     | applicant      | รศ. สมหญิง          |

## หมายเหตุสำหรับทีมพัฒนา

- **อย่าเผลอ Push ไฟล์ `database.db`**: ไฟล์นี้ถูกใส่ไว้ใน `.gitignore` แล้ว
- **หากมีการเพิ่ม User หรือแก้ไขเกณฑ์**: ให้แก้ไขไฟล์ในโฟลเดอร์ `backup/` แล้วรัน `migrate_data.py` ใหม่ครับ
